; Increase the maximum allowed size for uploaded files.
upload_max_filesize = 500M

; Increase the maximum allowed size for post data.
post_max_size = 500M

; Ensure memory limit is high enough to handle large uploads.
memory_limit = 1G